# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'InvoiceItems', type: :request do
  # describe 'GET /api/v1/invoices/:invoice_id/invoice_items' do
  #   it 'returns a list of invoice items for a specific invoice' do
  #     user = create(:user)
  #     invoice = create(:invoice, user: user)
  #     create_list(:invoice_item, 3, invoice: invoice)

  #     get "/api/v1/invoices/#{invoice.id}/invoice_items"

  #     expect(response).to have_http_status(:success)
  #     expect(json_response['data'].size).to eq(3)
  #   end
  # end
end
